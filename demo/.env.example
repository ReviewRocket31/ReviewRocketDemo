# Email Configuration (using nodemailer with SMTP)
# Required for email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-or-oauth-token

# Email addresses
OWNER_EMAIL=your-business-email@example.com
FROM_EMAIL=your-verified-sender@example.com

# Optional: Webhook URL for additional integrations
# Use services like Zapier, Make.com, or custom endpoint
# WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/123456/abcdef/

# Common SMTP Settings:
# Gmail: smtp.gmail.com:587 (use app password)
# Outlook: smtp-mail.outlook.com:587
# Yahoo: smtp.mail.yahoo.com:587
# Custom: your-mail-server.com:587